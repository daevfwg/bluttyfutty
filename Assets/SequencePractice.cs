using UnityEngine;

class SequencePractice : MonoBehaviour
{
    [SerializeField] int num;
    [SerializeField] int countOfDividers;

    void OnValidate()
    {
        while (num <= num)
        {

            int count = 0;
            Debug.Log(num );
            count++;

        }

    }
}
